# Repository Agent Rules

These rules apply to all Codex windows working in this repository.

## Git Flow (Mandatory)

1. Any code change must be done in a branch named `p0/*` or `fix/*`.
2. Do not commit directly to `main`.
3. Push branch changes to `origin`.
4. Open a Pull Request.
5. Merge only through PR flow.

